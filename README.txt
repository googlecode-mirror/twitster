INSTALLATION

1. Run the following commands:

prompt> cd $TWITSTER_HOME
prompt> mysqladmin create twitster
prompt> mysql twitster < sql/hashmt.sql

2. Edit your config.php file with the proper values

3. Make sure the cache directory is writable.
